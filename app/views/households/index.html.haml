%h1 Listing Households
%article
  = link_to 'New Household', new_household_path, class: :button, method: :get
  %ul
    - @households.each do |household| 
      %li
        = link_to 'X', household, method: :delete, data: { confirm: 'Are you sure?' } 
        &nbsp;
        = link_to household.name, household
  = link_to 'New Household', new_household_path, class: :button, method: :get
