= flash[:alert] if flash[:alert]
= form_for(@path) do |f|
  %h2
    Foods Eaten

  .row{id: "add_food_eaten"}
    = render partial: 'consumed_food', collection: @interview.consumed_foods
    #consumed_foods
      %h4
        1.) Search for the food item(s) you&apos;d like to add. (Don&apos;t push enter to search - it will cause problems)
        
      %input{type: "search", placeholder: "Search for food", id: "consumed_food_search", class: ['textfield', "col-sm-5"]}
      %input{type: "button", value: "Search!", class: [:btn, "btn-large", "btn-success", "col-sm-2"]}
  .row
    #display

  - if false
    %h2
      Foods Stored

    .row{id: "add_food_stored"}
      %input{type: "button", value: "Add New Food Stored"}
      

    %h2
      Foods Purchased

    .row{id: "add_food_purchase"}
      %input{type: "button", value: "Add Food Purchased"}

  .row
    %h4
      2.) When you have finished adding all the food items to the interview submit the form.
    = f.submit
